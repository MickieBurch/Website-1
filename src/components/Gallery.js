import React from "react";

const Gallery = () =>{

    return(
        <div>
            <h1>
                This is the gallery page.
            </h1>
        </div>

    );

}

export default Gallery